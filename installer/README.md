# Plane Manager Installer

An Electron app which does only one thing and one thing only: bootstraps Plane
Manager on the first server, then opens the remote in the browser, ready to go.

### Steps

1. Generate SSH key pair (crypto + sshpk)

### Notes

- Allows selecting which platform to install to, offering some common options
- The initial Plane Manager will have a raw IP address right after install
